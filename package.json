{
  "name": "pocket-pitlane",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "playwright test",
    "test:claims": "playwright test --grep @claim:",
    "test:realtime": "npm --prefix realtime ci && npm --prefix realtime test",
    "test:realtime:room-limit": "npm --prefix realtime ci && npm --prefix realtime run test:room-limit",
    "test:realtime:room-expiry": "npm --prefix realtime ci && npm --prefix realtime run test:room-expiry",
    "test:realtime:persistence": "npm --prefix realtime ci && npm --prefix realtime run test:persistence",
    "test:realtime:rate-limit": "npm --prefix realtime ci && npm --prefix realtime run test:rate-limit",
    "test:realtime:storage-scope": "npm --prefix realtime ci && npm --prefix realtime run test:storage-scope",
    "check": "npm run build && npm test && npm run test:realtime"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.11.0",
    "@playwright/test": "1.58.2",
    "typescript": "5.9.3",
    "vite": "7.1.7"
  }
}
